# MurderBoard UI/UX 修复完成报告模板

本文档用于其他 agent 完成 MurderBoard UI/UX 修复后，提交给 Codex 做上线前审查和核验。

请不要只写“已修复”。必须按本模板填写完整，否则审查者无法判断是否真的达到上线标准。

## 0. 审查目标

本报告用于提交给 Codex 做上线前 UI/UX 审查和工程核验。

本轮工作范围限定为 MurderBoard 首版：

- 桌游玩家个人临场使用。
- 中文可视化推理案件板。
- 本地优先 IndexedDB。
- 不做多人协作、账号、后端、主持人端、真相库、OCR/附件、AI 推理。

请审查者重点判断：

- 是否仍然符合“玩家临场推理案件板”的定位。
- 是否满足 Board 上线体验。
- 是否有阻断上线的问题。

## 1. 本轮目标

请用 1-3 句话说明本轮修复目标。

示例：

> 本轮完成 MurderBoard Board 页的 UI/UX 上线打磨：补齐 Board 内删除、四类节点 Inspector 编辑、中文错误反馈、搜索定位和平板布局。目标是让玩家不离开 Board 就能完成记录、关联、编辑、删除和找回关键推理。

本轮目标：

- 

## 2. 完成摘要

请用简短 bullet 总结本轮完成内容。

示例：

- 完成空状态一键创建默认案件。
- 完成四类节点在 Inspector 中编辑。
- 完成节点和关系删除。
- 完成搜索后自动选中匹配节点。
- 修复 900px 平板横向滚动。
- 增加中文错误反馈。
- 补充对应测试。

本轮完成：

- 
- 
- 

## 3. P0 完成项

### 3.1 空状态到建板路径

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

改动文件：

- `...`

行为说明：

- 空库进入 `/board` 时看到什么：
- 用户点击什么创建默认案件：
- 创建后是否立即进入 Board：
- 没有案件时快速录入是否禁用或有明确提示：

测试覆盖：

- 测试文件：
- 测试名称：
- 覆盖场景：

备注：

- 

### 3.2 Board 内删除

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

改动文件：

- `...`

行为说明：

- 如何删除人物节点：
- 如何删除线索节点：
- 如何删除事件节点：
- 如何删除猜想节点：
- 如何删除关系：
- 删除后关系如何安全处理：
- 刷新后已删除项是否隐藏：

测试覆盖：

- 测试文件：
- 测试名称：
- 覆盖场景：

备注：

- 

### 3.3 四类节点 Inspector 编辑

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

改动文件：

- `...`

行为说明：

- 人物可编辑字段：
  - `name`
  - `role`
  - `notes`
- 线索可编辑字段：
  - `title`
  - `source`
  - `content`
- 事件可编辑字段：
  - `title`
  - `occurredAt`
  - `description`
- 猜想可编辑字段：
  - `title`
  - `body`
  - `status`
  - `confidence`
- 保存后画布是否立即更新：
- 刷新后是否保留：

测试覆盖：

- 测试文件：
- 测试名称：
- 覆盖场景：

备注：

- 

### 3.4 中文错误反馈

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

改动文件：

- `...`

行为说明：

- 创建失败如何提示：
- 保存失败如何提示：
- 删除失败如何提示：
- 关系创建失败如何提示：
- 位置保存失败如何提示：
- 是否仍存在 `Unknown error`、`Action failed` 或英文后台错误：

测试覆盖：

- 测试文件：
- 测试名称：
- 覆盖场景：

备注：

- 

### 3.5 搜索找回体验

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

改动文件：

- `...`

行为说明：

- 搜索人物时发生什么：
- 搜索线索时发生什么：
- 搜索事件时发生什么：
- 搜索猜想时发生什么：
- 是否自动选中第一个匹配节点：
- Inspector 是否打开匹配节点：
- 非匹配节点是否 dim 或隐藏：
- 是否满足 10 秒找回目标：

测试覆盖：

- 测试文件：
- 测试名称：
- 覆盖场景：

备注：

- 

### 3.6 平板和桌面布局

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

改动文件：

- `...`

行为说明：

- 1440px 桌面布局：
- 900px 平板布局：
- 768px 平板布局：
- Inspector 在平板上如何展示：
- 是否存在横向滚动：
- 节点文字是否溢出或遮挡：
- 画布是否仍为主视觉：

测试覆盖或截图：

- 桌面截图：
- 平板截图：
- 空状态截图：
- 样例案件截图：

备注：

- 

## 4. P1 完成项

### 4.1 关系创建 target 搜索/过滤

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

说明：

- 

测试覆盖：

- 

### 4.2 Cases 辅助页最低限度中文化

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

说明：

- 哪些英文已改中文：
- 哪些英文暂未改：
- 为什么：

测试覆盖：

- 

### 4.3 视觉细节打磨

状态：

- [ ] 已完成
- [ ] 未完成
- [ ] 不适用

说明：

- 节点类型视觉：
- 关系线语义：
- 焦点态：
- 选中态：
- 禁用态：
- 深色背景对比度：

测试或截图：

- 

## 5. 未完成项

请列出本轮没有完成的内容，必须说明原因。

格式：

- 未完成：
  - 原因：
  - 是否阻断上线：
  - 建议后续处理：

示例：

- 未完成：Cases 页面全量重设计。
  - 原因：本轮只做 Board 上线打磨，Cases 是辅助入口。
  - 是否阻断上线：否。
  - 建议后续处理：MVP 后做管理页中文化和降后台感。

本轮未完成：

- 

## 6. 关键交互走查说明

请逐条说明现在用户怎么完成这些流程。

### 6.1 空库建板

步骤：

1. 
2. 
3. 

预期结果：

- 

### 6.2 创建四类节点

人物：

1. 
2. 

线索：

1. 
2. 

事件：

1. 
2. 

猜想：

1. 
2. 

预期结果：

- 

### 6.3 建立关系

建立 `supports`：

1. 
2. 
3. 

建立 `refutes`：

1. 
2. 
3. 

建立其他关系：

- `related`：
- `sequence`：
- `suspect`：

预期结果：

- 

### 6.4 编辑节点

编辑人物：

1. 
2. 

编辑线索：

1. 
2. 

编辑事件：

1. 
2. 

编辑猜想：

1. 
2. 

预期结果：

- 

### 6.5 删除节点和关系

删除节点：

1. 
2. 

删除关系：

1. 
2. 

预期结果：

- 

### 6.6 搜索并定位

步骤：

1. 输入搜索词：
2. 页面如何展示匹配：
3. 是否打开 Inspector：
4. 如何清除搜索：

预期结果：

- 

### 6.7 平板布局

步骤：

1. 使用 900px 或 768px 宽度打开 `/board`。
2. 检查顶部栏、快速录入、画布、Inspector。
3. 检查是否有横向滚动。

预期结果：

- 

## 7. 测试结果

请贴完整命令和结果摘要。

```bash
npm test
```

结果：

```text
...
```

```bash
npx vitest run src
```

结果：

```text
...
```

```bash
npm run build
```

结果：

```text
...
```

如果有失败，必须写：

- 失败命令：
- 失败测试：
- 失败原因：
- 是否已修复：
- 未修复原因：

## 8. 截图和预览

请提供截图路径或截图说明。

必需截图：

- 空状态截图：
  - 路径：
  - 视口：

- 样例案件桌面截图：
  - 路径：
  - 视口：建议 1440px

- 样例案件平板截图：
  - 路径：
  - 视口：建议 768px-1024px

- Inspector 编辑截图：
  - 路径：
  - 说明：

- 关系创建截图：
  - 路径：
  - 说明：

- 搜索定位截图：
  - 路径：
  - 说明：

本地预览地址：

- `http://127.0.0.1:5173/board`

## 9. Design Gate 自评

请按 0-2 分打分，总分 16 分。

| 项目 | 分数 | 说明 |
| --- | ---: | --- |
| 案件板主导性 | /2 | |
| 临场录入效率 | /2 | |
| 节点识别 | /2 | |
| 关系语义 | /2 | |
| 搜索找回 | /2 | |
| Inspector | /2 | |
| 响应式 | /2 | |
| 视觉基调 | /2 | |

总分：

- `__/16`

自评结论：

- [ ] Pass
- [ ] Blocked

如果 Blocked，阻断原因：

- 

## 10. 文案检查

请确认以下内容。

- [ ] Board 核心 UI 为中文。
- [ ] 错误提示为中文。
- [ ] 空状态是引导开始建板，不是解释一堆功能。
- [ ] 没有明显 `CRUD`、`management`、`entity`、`operation`、`Unknown error`、`Action failed` 等后台/英文文案。
- [ ] 猜想没有被当作事实或答案。
- [ ] 没有主持人真相、答案揭示、多人协作相关入口。

发现的问题：

- 

## 11. 风险和已知问题

必须明确写。没有就写“无已知阻断问题”。

阻断问题：

- 

非阻断问题：

- 

需要产品确认的问题：

- 

技术债：

- 

## 12. Git 信息

请贴下面两条命令的输出。

```bash
git log --oneline -5
```

输出：

```text
...
```

```bash
git status --short
```

输出：

```text
...
```

最新 commit：

- `...`

## 13. 给审查者的重点

请告诉 Codex 重点核验哪些地方。

示例：

- 请重点检查 900px 平板是否还有横向滚动。
- 请重点检查删除关系后刷新是否稳定。
- 请重点检查中文错误提示是否还有英文残留。
- 请重点检查搜索是否能打开 Inspector。
- 请重点检查空状态创建默认案件后能否继续快速录入。

本轮请重点核验：

- 
- 
- 

## 14. 最终声明

请确认：

- [ ] 本轮没有新增多人协作。
- [ ] 本轮没有新增账号/后端/同步。
- [ ] 本轮没有新增主持人端。
- [ ] 本轮没有新增真相库/答案揭示。
- [ ] 本轮没有新增 OCR/附件/拍照。
- [ ] 本轮没有新增 AI 推理凶手。
- [ ] 本轮没有进行不必要的大重构。
- [ ] `npm test` 通过。
- [ ] `npx vitest run src` 通过。
- [ ] `npm run build` 通过。

提交人：

- 

提交时间：

- 
