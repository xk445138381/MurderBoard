# MurderBoard ·领域术语 (Ubiquitous Language)

> 仅定义领域概念和约束，不含实现细节。

## 核心概念

**内容类型模板 (Template)**: 全局可复用类型定义，存于模板库。创建后不可修改，被使用时深拷贝。
- 定义：类型名称、一组字段（字段名、字段类型、必填标记）、板面视觉（颜色、形状）、是否可内嵌解锁块

**游戏 (Game)**: 顶层容器。创建时选取模板深拷贝合并。
- 模板合并：同名且同字段模式（仅比较字段名+类型）→ 自动合并；字段不同 → 提示重命名

**内容类型 (Content Type)**: 游戏内专有实体模板，深拷贝自模板，可在本局自由修改。

**内容项 (Content Item)**: 内容类型实例。

**字段类型池**: 单行文本 / 多行文本 / 日期时间 / 日期范围 / 数字 / 枚举 / 布尔 / 引用 / 解锁块。

**解锁块 (Unlock Block)**: 复合字段。解锁目标名称、所需人员(可选)、所需前往(可选)、获取状态（未解锁 / 已解锁未获取 / 已获取）。

**推理关系 (Board Relation)**: `related` / `supports` / `refutes` / `sequence` / `suspect`。玩家手动建立。

**解锁关系 (Unlock Relation)**: 解锁块自动生成的有向连线，独立于推理关系。

## 板面结构

- **子板面**: 创建诉状案卷时自动生成，展示该案卷及其解锁链内容
- **跨案卷总板面**: 一场游戏一个
- 内容项可同时存在于多个板面（位置数据按板面独立，内容数据全局唯一）
- 节点：颜色+形状区分内容类型；三种解锁状态视觉不同

## 解锁匹配

优先名称自动匹配，匹配不到则手动选择。

## 根本规则

- 原始模板不可修改
- 用户只记录已知信息，不保存主持人答案或未公开线索
- 跨诉状案卷关联由玩家推断
- 删除节点从板面移除位置，永久删除需显式操作